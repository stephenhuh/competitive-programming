var line;
let numCircles;
let input = [];

// [3 E right]
// {question: attempts}
while (line = readline()) {
	let nums = line.split(' ');
	input.push(nums);
}

numCircles = input[0][0];
input


input.forEach(function(el, idx, arr){
});

print(primary, secondary);


//distance between centers must be less than sum of radii 
function hasCollisionX(c1, c2){
	computeDistance(c1[0], c2[0], c1[1], c2[1]);

}




function computeDistance (x1, x2, y1, y2) {
	return Math.sqrt(Math.pow(y2 - y1, 2) + Math.pow(x2 - x1, 2));
}
